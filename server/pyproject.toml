[project]
name = "rag-notebook-server"
version = "0.1.0"
description = "RAG Notebook server — FastAPI + pgvector + BGE-M3"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 120
