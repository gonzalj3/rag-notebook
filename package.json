{
  "name": "rag-notebook",
  "private": true,
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && python -m uvicorn app.main:app --reload",
    "dev": "npm run client"
  }
}
